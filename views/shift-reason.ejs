<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Shift reason - CareShiftAI</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header>
    <h1>Tell us why</h1>
  </header>

  <main>
    <h2><%= action === 'clock-in' ? 'Clocking in late' : 'Clocking out early/late' %></h2>
    <p>
      Shift on <strong><%= shift.date %></strong>,
      <strong><%= shift.start_time %></strong>–<strong><%= shift.end_time %></strong>,
      at <strong><%= shift.location %></strong>.
    </p>

    <form method="POST"
          action="/shifts/<%= shift.id %>/<%= action === 'clock-in' ? 'clock-in' : 'clock-out' %>">
      <label for="reason">Please give a brief reason (required):</label>
      <textarea id="reason" name="reason" rows="4" required></textarea>

      <button type="submit" class="btn-primary">Submit reason &amp; continue</button>
      <a href="/my-shifts" class="link-secondary">Cancel</a>
    </form>
  </main>
</body>
</html>
